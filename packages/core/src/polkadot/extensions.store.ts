// import { store } from "@/api/store";
// import { map, shareReplay } from "rxjs";

// // TODO rename, they actually might not be connected
// export const polkadotStoredEnabledExtensionIds$ = store.observable.pipe(
//   map((s) => s.polkadot?.enabledExtensionIds ?? []),
//   shareReplay(1)
// );
